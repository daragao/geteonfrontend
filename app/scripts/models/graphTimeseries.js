/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var GraphtimeseriesModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return GraphtimeseriesModel;
});
